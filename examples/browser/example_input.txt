-- create tables
create table test (
    id                             number generated by default on null as identity 
                                   constraint test_id_pk primary key,
    name                           varchar2(255 char),
    number3_header                 number,
    date1_header                   date,
    sample_fk                      number
                                   constraint products_fals_products_type_fk
                                   references product_types on delete cascade not null,
)
;

-- table index
create index test_i1 on test (name);

--comments on tables
comment on table test is 'This is test table';
comment on column test.name is 'Name to display to user';